#include <stdio.h>

int oppgave_6() {
	 // char this is a comment
	 char *pszB = "Hello, World!";
	 int a;
	 for (a = 0; a < 1; a++) {
		  printf("%d\n", a+1);
		  // This is another comment }
	 }
	 int c;
	 for	 (c = 0; c < 1; c++)	  {
		  printf("%d\n", c+1);
		  // This is also a comment }



	 }
	 char *pszNokka = "nokka";
	return 0;
}