#include <stdio.h>

#include "../include/oppgave6_test.h"

int oppgave_6() {
    // char this is a comment
    char *b = "Hello, World!";
    int a;
    a = 0;
    while (a < 10) {
        printf("%d\n", a+1);
        // This is another comment }
        a++;
    }
    char *nokka = "nokka";
   return 0;
}